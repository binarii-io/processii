auto-install-peers=true
strict-peer-dependencies=false
# Hoist shared dev tooling (typescript, vitest, eslint) to the root node_modules
# so every workspace package resolves them without re-declaring.
shamefully-hoist=true
